doctype html
html(lang="en")
    head
        include parciales/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina 
            p Agrega tu e-mail para reestablecer tu contraseña

            if locals.mensajes 
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form(action="/reestablecer" method="POST").caja-login
                .campo 
                    label(for="email") E-mail:
                    input#email(type="email", name="email" placeholder="Tu Email" value= email)
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer Password")
                .campo.acciones 
                    a(href="/iniciar-sesion") Volver al Login